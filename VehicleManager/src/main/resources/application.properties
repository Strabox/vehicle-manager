#================================
#			APPLICATION
#NOTE: This properties are a MUST!!!
#================================

application.https = false

#================================
#			SPRING
#================================

security.user.password = c005f9f6-4738-4636-ad25-7e68e9b50651
# Web-server port (if not overrided in code!!!)
spring.server.port = 8080

# ===============================
# 		 DATA SOURCE
# ===============================

spring.datasource.url = jdbc:mysql://localhost:3307/vehiclemanager
spring.datasource.driver-class-name = com.mysql.jdbc.Driver
spring.datasource.username = root
spring.datasource.password =
# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# ===============================
# 		 JPA / HIBERNATE
# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).
# ===============================

# Enable JPA SQL queries log.
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in the project
spring.jpa.hibernate.ddl-auto = create
# Naming strategy
#spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
# ===============================
# ===============================